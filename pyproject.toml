[project]
name = "br-community-mcp"
version = "0.1.0"
description = "MCP server for searching the B&R Automation community forum"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
    "mcp[cli]>=1.25.0",
]

[project.scripts]
br-community-mcp = "server:mcp.run"
